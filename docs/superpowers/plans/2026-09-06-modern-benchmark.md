# Modern Serialization Benchmark Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the legacy serializer script with a reproducible, correctness-gated, two-tier benchmark of maintained Python serialization libraries.

**Architecture:** A typed adapter registry presents one canonical dataclass workload to primitive and encoded serializers. Validation proves semantic equivalence before a `pyperf` runner times independent operations, and a report generator turns raw JSON into format-partitioned Markdown and HTML without combining incomparable results.

**Tech Stack:** Python 3.12–3.14, uv/`uv_build`, pytest, pyperf, stdlib dataclasses, Marshmallow, Django REST Framework, cattrs, mashumaro, msgspec, Pydantic v2, serpyco-rs, Adaptix, orjson, ormsgpack, msgpack, Avro, Docker, GitHub Actions, GitHub Pages.

**Spec:** `docs/superpowers/specs/2026-09-06-modern-benchmark-design.md`

## Global Constraints

- Support Python `>=3.12,<3.15`; Python 3.14 is the canonical benchmark and container interpreter.
- Manage the project only through `pyproject.toml`, `uv.lock`, and `uv`. Remove `requirements.txt`.
- Use the shared mutable stdlib `Child` and `Parent` dataclasses for every primary comparison.
- The batch workload contains exactly 100 semantically distinct `Parent` instances.
- Keep schema creation, converter creation, code generation, and encoded-input preparation outside timed loops.
- Keep garbage collection enabled and record `gc=enabled` in benchmark metadata.
- Validate every supported operation before timing it.
- Never combine primitive, JSON, MessagePack, Avro, or pickle measurements.
- Never combine dump/load or single/batch measurements into a composite score.
- Do not use `Any` in project annotations. Use `object` at third-party boundaries and narrow or cast at those boundaries.
- Tests use real serializer APIs and real bytes. Do not mock serializers.
- Remove Serpy, old serpyco, Strainer, Lollipop, Kim, Toasted Marshmallow, Colander, Lima, and all compatibility shims. Do not create a legacy profile or historical leaderboard.
- Pin these currently verified releases: Marshmallow 4.3.1, Django REST Framework 3.18.0, Django 6.1.1, msgspec 0.21.1, mashumaro 3.22, cattrs 26.2.0, Pydantic 2.13.5, serpyco-rs 1.22.0, Adaptix 3.0.0b12, orjson 3.12.0, ormsgpack 1.12.2, msgpack 1.2.2, Avro 1.12.2, pyperf 2.10.0, and pytest 9.1.1.
- Label Adaptix 3.0.0b12 as a prerelease in metadata and reports.

---

## File Map

### Create

- `pyproject.toml`: package metadata, exact dependencies, console entry point, pytest configuration.
- `uv.lock`: cross-platform exact dependency resolution generated by uv.
- `.python-version`: canonical local interpreter, `3.14`.
- `serialization_benchmark/__init__.py`: package version lookup.
- `serialization_benchmark/__main__.py`: module entry point.
- `serialization_benchmark/contracts.py`: JSON types, operation literals, metadata, primitive and encoded protocols.
- `serialization_benchmark/fixtures.py`: canonical dataclasses, deterministic fixtures, and handwritten primitive conversion.
- `serialization_benchmark/registry.py`: explicit primitive and encoded adapter registries.
- `serialization_benchmark/validation.py`: exact correctness checks and diagnostics.
- `serialization_benchmark/runner.py`: benchmark case preparation and `pyperf` execution.
- `serialization_benchmark/reporting.py`: raw-result parsing and Markdown/HTML rendering.
- `serialization_benchmark/report.css`: dependency-free report styling.
- `serialization_benchmark/adapters/__init__.py`: adapter package marker.
- `serialization_benchmark/adapters/baselines.py`: handwritten and `dataclasses.asdict` primitive baselines.
- `serialization_benchmark/adapters/marshmallow.py`: Marshmallow primitive adapter.
- `serialization_benchmark/adapters/drf.py`: Django REST Framework primitive adapter.
- `serialization_benchmark/adapters/cattrs.py`: cattrs primitive adapter.
- `serialization_benchmark/adapters/mashumaro.py`: mashumaro primitive adapter.
- `serialization_benchmark/adapters/msgspec.py`: msgspec primitive adapter.
- `serialization_benchmark/adapters/pydantic.py`: Pydantic primitive adapter.
- `serialization_benchmark/adapters/serpyco_rs.py`: serpyco-rs primitive adapter.
- `serialization_benchmark/adapters/adaptix.py`: Adaptix primitive adapter.
- `serialization_benchmark/adapters/encoded_json.py`: JSON byte adapters.
- `serialization_benchmark/adapters/encoded_msgpack.py`: MessagePack byte adapters.
- `serialization_benchmark/adapters/encoded_avro.py`: Avro byte adapter and schema.
- `serialization_benchmark/adapters/encoded_pickle.py`: pickle byte adapter.
- `tests/test_fixtures.py`: deterministic fixture tests.
- `tests/test_registry.py`: stable adapter registration tests.
- `tests/test_primitive_adapters.py`: parametrized primitive contract tests.
- `tests/test_encoded_adapters.py`: parametrized encoded contract tests.
- `tests/test_validation.py`: failure diagnostic tests.
- `tests/test_runner.py`: case construction and benchmark smoke tests.
- `tests/test_reporting.py`: raw JSON parsing and table partition tests.
- `tests/test_cli.py`: subprocess-level CLI tests.
- `tests/fixtures/pyperf-smoke.json`: deterministic two-benchmark report fixture.
- `.github/workflows/ci.yml`: Python 3.12–3.14 correctness and smoke checks.
- `.github/workflows/benchmark-pages.yml`: manual canonical run, artifacts, and Pages deployment.
- `results/raw/.gitkeep` and `results/reports/.gitkeep`: result directory anchors.

### Modify

- `README.md`: current scope, methodology, commands, result interpretation, and safety notes.
- `Dockerfile`: pinned uv, locked Python 3.14 environment, console entry point.
- `docker-compose.yml`: validate, tests, primitive, and encoded services.
- `.gitignore`: build, cache, environment, and transient result exclusions without ignoring published results.

### Delete

- `benchmark.py`
- `data.py`
- `requirements.txt`
- `subjects/`
- `tests/test_compatibility.py`
- `tests/test_serializers.py`
- `tests/__init__.py`
- `.travis.yml`
- `create_report.sh`
- `deploy.sh`
- `deploy-key.enc`
- `disscussion.md`

---

### Task 1: Atomic Baseline Cutover

**Files:**
- Create: `pyproject.toml`
- Create: `uv.lock`
- Create: `.python-version`
- Create: `serialization_benchmark/__init__.py`
- Create: `serialization_benchmark/__main__.py`
- Create: `serialization_benchmark/cli.py`
- Create: `serialization_benchmark/contracts.py`
- Create: `serialization_benchmark/fixtures.py`
- Create: `serialization_benchmark/registry.py`
- Create: `serialization_benchmark/validation.py`
- Create: `serialization_benchmark/adapters/__init__.py`
- Create: `serialization_benchmark/adapters/baselines.py`
- Create: `tests/test_fixtures.py`
- Create: `tests/test_registry.py`
- Create: `tests/test_primitive_adapters.py`
- Create: `tests/test_validation.py`
- Create: `tests/test_cli.py`
- Create: `.github/workflows/ci.yml`
- Modify: `README.md`
- Modify: `Dockerfile`
- Modify: `docker-compose.yml`
- Modify: `.gitignore`
- Delete: `benchmark.py`
- Delete: `data.py`
- Delete: `requirements.txt`
- Delete: `subjects/`
- Delete: `tests/test_compatibility.py`
- Delete: `tests/test_serializers.py`
- Delete: `tests/__init__.py`
- Delete: `.travis.yml`
- Delete: `create_report.sh`
- Delete: `deploy.sh`
- Delete: `deploy-key.enc`
- Delete: `disscussion.md`

**Interfaces:**
- Produces: `Child`, `Parent`, `FixtureSet`, `make_fixtures(batch_size: int = 100) -> FixtureSet`.
- Produces: `PrimitiveAdapter` and `EncodedAdapter` protocols plus `AdapterMetadata`.
- Produces: `primitive_adapters() -> tuple[PrimitiveAdapter, ...]` and `encoded_adapters() -> tuple[EncodedAdapter, ...]`.
- Produces: `validate_primitive_adapter(adapter, fixtures)` and `validate_all()`.
- Produces: `serialization-benchmark validate`.

- [ ] **Step 1: Add package metadata and failing fixture tests**

Create `pyproject.toml`:

```toml
[project]
name = "python-serialization-benchmark"
version = "0.1.0"
description = "A reproducible benchmark of maintained Python serialization libraries"
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = []

[project.scripts]
serialization-benchmark = "serialization_benchmark.cli:main"

[dependency-groups]
dev = ["pytest==9.1.1"]

[build-system]
requires = ["uv_build>=0.12.10,<0.13"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "serialization_benchmark"
module-root = ""

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
```

Create `.python-version` containing `3.14`. Add `.venv/`, `.pytest_cache/`, `__pycache__/`, `*.py[cod]`, `dist/`, and `build/` to `.gitignore`.

Create `tests/test_fixtures.py`:

```python
from serialization_benchmark.fixtures import (
    Child,
    Parent,
    make_fixtures,
    parent_from_primitive,
    parent_to_primitive,
)


def test_fixture_set_is_deterministic_and_has_100_distinct_parents() -> None:
    first = make_fixtures()
    second = make_fixtures()

    assert first == second
    assert len(first.many) == 100
    assert len(set(parent.foo for parent in first.many)) == 100
    assert first.one == first.many[0]


def test_fixture_round_trip_is_exact() -> None:
    fixtures = make_fixtures()

    assert parent_from_primitive(parent_to_primitive(fixtures.one)) == fixtures.one
    assert fixtures.one == Parent(
        foo="parent-0",
        count=0,
        active=True,
        ratio=0.5,
        sub=Child(w=100, x=20, y="hello-0", z=10),
        subs=[Child(w=100 + index, x=20 + index, y=f"hello-{index}", z=10 + index) for index in range(10)],
    )
```

- [ ] **Step 2: Lock and verify the fixture tests fail**

Run:

```bash
uv lock
uv run pytest tests/test_fixtures.py -v
```

Expected: collection fails with `ModuleNotFoundError: No module named 'serialization_benchmark'`.

- [ ] **Step 3: Implement typed contracts and canonical fixtures**

Create `serialization_benchmark/contracts.py` with these public types:

```python
from __future__ import annotations

from dataclasses import dataclass
from typing import TYPE_CHECKING, Literal, Protocol, Sequence

if TYPE_CHECKING:
    from serialization_benchmark.fixtures import Parent

type JsonScalar = None | bool | int | float | str
type JsonValue = JsonScalar | list[JsonValue] | dict[str, JsonValue]
type JsonObject = dict[str, JsonValue]
type PrimitiveOperation = Literal["dump_one", "load_one", "dump_many", "load_many"]
type EncodedOperation = Literal["encode_one", "decode_one", "encode_many", "decode_many"]
type EncodedFormat = Literal["json", "messagepack", "avro", "pickle"]
type ModelStrategy = Literal["dataclass"]

ALL_PRIMITIVE_OPERATIONS: frozenset[PrimitiveOperation] = frozenset(
    {"dump_one", "load_one", "dump_many", "load_many"}
)
ALL_ENCODED_OPERATIONS: frozenset[EncodedOperation] = frozenset(
    {"encode_one", "decode_one", "encode_many", "decode_many"}
)


@dataclass(frozen=True)
class AdapterMetadata:
    slug: str
    name: str
    package: str
    version: str
    model_strategy: ModelStrategy = "dataclass"
    prerelease: bool = False


class PrimitiveAdapter(Protocol):
    metadata: AdapterMetadata
    operations: frozenset[PrimitiveOperation]

    def dump_one(self, value: object) -> JsonObject: ...
    def load_one(self, value: JsonObject) -> object: ...
    def dump_many(self, values: Sequence[object]) -> list[JsonObject]: ...
    def load_many(self, values: Sequence[JsonObject]) -> list[object]: ...
    def normalize(self, value: object) -> Parent: ...


class EncodedAdapter(Protocol):
    metadata: AdapterMetadata
    format: EncodedFormat
    operations: frozenset[EncodedOperation]

    def encode_one(self, value: object) -> bytes: ...
    def decode_one(self, value: bytes) -> object: ...
    def encode_many(self, values: Sequence[object]) -> bytes: ...
    def decode_many(self, value: bytes) -> list[object]: ...
    def normalize(self, value: object) -> Parent: ...
```

Create `serialization_benchmark/fixtures.py`:

```python
from __future__ import annotations

from dataclasses import dataclass

from serialization_benchmark.contracts import JsonObject, JsonValue


@dataclass
class Child:
    w: int
    x: int
    y: str
    z: int


@dataclass
class Parent:
    foo: str
    count: int
    active: bool
    ratio: float
    sub: Child
    subs: list[Child]


@dataclass(frozen=True)
class FixtureSet:
    one: Parent
    many: tuple[Parent, ...]
    one_primitive: JsonObject
    many_primitives: tuple[JsonObject, ...]


def make_child(seed: int) -> Child:
    return Child(w=100 + seed, x=20 + seed, y=f"hello-{seed}", z=10 + seed)


def make_parent(seed: int) -> Parent:
    return Parent(
        foo=f"parent-{seed}",
        count=seed,
        active=seed % 2 == 0,
        ratio=seed + 0.5,
        sub=make_child(seed),
        subs=[make_child(seed * 10 + index) for index in range(10)],
    )


def child_to_primitive(value: Child) -> JsonObject:
    return {"w": value.w, "x": value.x, "y": value.y, "z": value.z}


def parent_to_primitive(value: Parent) -> JsonObject:
    return {
        "foo": value.foo,
        "count": value.count,
        "active": value.active,
        "ratio": value.ratio,
        "sub": child_to_primitive(value.sub),
        "subs": [child_to_primitive(child) for child in value.subs],
    }


def _require_int(value: JsonValue) -> int:
    if type(value) is not int:
        raise TypeError(f"expected int, got {type(value).__name__}")
    return value


def _require_bool(value: JsonValue) -> bool:
    if type(value) is not bool:
        raise TypeError(f"expected bool, got {type(value).__name__}")
    return value


def _require_float(value: JsonValue) -> float:
    if type(value) is not float:
        raise TypeError(f"expected float, got {type(value).__name__}")
    return value


def _require_str(value: JsonValue) -> str:
    if not isinstance(value, str):
        raise TypeError(f"expected str, got {type(value).__name__}")
    return value


def _child_from_object(value: JsonValue) -> Child:
    if not isinstance(value, dict):
        raise TypeError(f"expected child dictionary, got {type(value).__name__}")
    return Child(
        w=_require_int(value["w"]),
        x=_require_int(value["x"]),
        y=_require_str(value["y"]),
        z=_require_int(value["z"]),
    )


def parent_from_primitive(value: JsonObject) -> Parent:
    raw_subs = value["subs"]
    if not isinstance(raw_subs, list):
        raise TypeError("expected subs to be a list")
    return Parent(
        foo=_require_str(value["foo"]),
        count=_require_int(value["count"]),
        active=_require_bool(value["active"]),
        ratio=_require_float(value["ratio"]),
        sub=_child_from_object(value["sub"]),
        subs=[_child_from_object(child) for child in raw_subs],
    )


def normalize_parent(value: object) -> Parent:
    if not isinstance(value, Parent):
        raise TypeError(f"expected Parent, got {type(value).__name__}")
    return value


def make_fixtures(batch_size: int = 100) -> FixtureSet:
    if batch_size != 100:
        raise ValueError("the canonical batch size is 100")
    many = tuple(make_parent(index) for index in range(batch_size))
    primitives = tuple(parent_to_primitive(parent) for parent in many)
    return FixtureSet(
        one=many[0],
        many=many,
        one_primitive=primitives[0],
        many_primitives=primitives,
    )
```

Create `serialization_benchmark/__init__.py` using `importlib.metadata.version` with a `PackageNotFoundError` fallback of `"0.1.0"`.

- [ ] **Step 4: Run fixture tests**

Run: `uv run pytest tests/test_fixtures.py -v`

Expected: 2 tests pass.

- [ ] **Step 5: Add failing baseline, registry, validation, and CLI tests**

Create `tests/test_registry.py`:

```python
from serialization_benchmark.registry import encoded_adapters, primitive_adapters


def test_primitive_registry_order() -> None:
    assert [adapter.metadata.slug for adapter in primitive_adapters()] == [
        "handwritten",
        "dataclasses-asdict",
    ]
    assert encoded_adapters() == ()
```

Create `tests/test_primitive_adapters.py`:

```python
import pytest

from serialization_benchmark.fixtures import make_fixtures
from serialization_benchmark.contracts import PrimitiveAdapter
from serialization_benchmark.registry import primitive_adapters
from serialization_benchmark.validation import validate_primitive_adapter


@pytest.mark.parametrize(
    "adapter",
    primitive_adapters(),
    ids=lambda adapter: adapter.metadata.slug,
)
def test_registered_primitive_adapter_is_semantically_correct(
    adapter: PrimitiveAdapter,
) -> None:
    validate_primitive_adapter(adapter, make_fixtures())
```

Create `tests/test_validation.py` with a local adapter whose `dump_one` returns an invalid result:

```python
import pytest

from serialization_benchmark.adapters.baselines import HandwrittenAdapter
from serialization_benchmark.contracts import AdapterMetadata, JsonObject
from serialization_benchmark.fixtures import make_fixtures
from serialization_benchmark.validation import AdapterValidationError, validate_primitive_adapter


class BrokenAdapter(HandwrittenAdapter):
    metadata = AdapterMetadata(
        slug="broken",
        name="Broken",
        package="test",
        version="1",
    )

    def dump_one(self, value: object) -> JsonObject:
        return {"wrong": True}


def test_validation_names_the_broken_operation() -> None:
    with pytest.raises(AdapterValidationError, match="broken.dump_one"):
        validate_primitive_adapter(BrokenAdapter(), make_fixtures())
```

Create `tests/test_cli.py`:

```python
import subprocess


def test_validate_command_succeeds() -> None:
    completed = subprocess.run(
        ["serialization-benchmark", "validate"],
        check=False,
        capture_output=True,
        text=True,
    )

    assert completed.returncode == 0
    assert completed.stdout.startswith("validation passed:")
```

- [ ] **Step 6: Verify the new tests fail**

Run:

```bash
uv run pytest tests/test_registry.py tests/test_primitive_adapters.py tests/test_validation.py tests/test_cli.py -v
```

Expected: collection fails because `registry`, `validation`, `adapters.baselines`, and `cli` do not exist.

- [ ] **Step 7: Implement baselines, registry, validation, and CLI**

Create baseline adapters with all four methods present. `HandwrittenAdapter` delegates to `parent_to_primitive` and `parent_from_primitive`. `DataclassesAsdictAdapter` declares only `dump_one` and `dump_many` and raises `NotImplementedError` from both load methods. Both use `normalize_parent`.

The essential adapter definitions are:

```python
@dataclass(frozen=True)
class HandwrittenAdapter:
    metadata = AdapterMetadata(
        slug="handwritten",
        name="Handwritten",
        package="python",
        version=platform.python_version(),
    )
    operations = ALL_PRIMITIVE_OPERATIONS

    def dump_one(self, value: object) -> JsonObject:
        return parent_to_primitive(cast(Parent, value))

    def load_one(self, value: JsonObject) -> object:
        return parent_from_primitive(value)

    def dump_many(self, values: Sequence[object]) -> list[JsonObject]:
        return [parent_to_primitive(cast(Parent, value)) for value in values]

    def load_many(self, values: Sequence[JsonObject]) -> list[object]:
        return [parent_from_primitive(value) for value in values]

    def normalize(self, value: object) -> Parent:
        return normalize_parent(value)


@dataclass(frozen=True)
class DataclassesAsdictAdapter:
    metadata = AdapterMetadata(
        slug="dataclasses-asdict",
        name="dataclasses.asdict",
        package="python",
        version=platform.python_version(),
    )
    operations: frozenset[PrimitiveOperation] = frozenset({"dump_one", "dump_many"})

    def dump_one(self, value: object) -> JsonObject:
        return cast(JsonObject, asdict(cast(Parent, value)))

    def dump_many(self, values: Sequence[object]) -> list[JsonObject]:
        return [cast(JsonObject, asdict(cast(Parent, value))) for value in values]
```

Create an explicit registry:

```python
_PRIMITIVE_ADAPTERS: tuple[PrimitiveAdapter, ...] = (
    HandwrittenAdapter(),
    DataclassesAsdictAdapter(),
)
_ENCODED_ADAPTERS: tuple[EncodedAdapter, ...] = ()


def primitive_adapters() -> tuple[PrimitiveAdapter, ...]:
    return _PRIMITIVE_ADAPTERS


def encoded_adapters() -> tuple[EncodedAdapter, ...]:
    return _ENCODED_ADAPTERS
```

`validate_primitive_adapter` must branch only on declared operations and compare exact objects:

```python
def validate_primitive_adapter(
    adapter: PrimitiveAdapter,
    fixtures: FixtureSet,
) -> None:
    slug = adapter.metadata.slug
    if "dump_one" in adapter.operations:
        _assert_equal(f"{slug}.dump_one", fixtures.one_primitive, adapter.dump_one(fixtures.one))
    if "load_one" in adapter.operations:
        loaded = adapter.normalize(adapter.load_one(fixtures.one_primitive))
        _assert_equal(f"{slug}.load_one", fixtures.one, loaded)
    if "dump_many" in adapter.operations:
        dumped = adapter.dump_many(list(fixtures.many))
        _assert_equal(f"{slug}.dump_many", list(fixtures.many_primitives), dumped)
    if "load_many" in adapter.operations:
        loaded = [adapter.normalize(value) for value in adapter.load_many(list(fixtures.many_primitives))]
        _assert_equal(f"{slug}.load_many", list(fixtures.many), loaded)
```

`AdapterValidationError` includes the operation name plus `expected=...` and `actual=...`. `validate_all()` validates every primitive adapter and, once encoded adapters exist, every encoded adapter.

Create `serialization_benchmark/cli.py` with `argparse`, a `validate` subcommand, and `main() -> int`. Create `__main__.py` that raises `SystemExit(main())`.

- [ ] **Step 8: Run all new tests**

Run:

```bash
uv run pytest tests/test_fixtures.py tests/test_registry.py tests/test_primitive_adapters.py tests/test_validation.py tests/test_cli.py -v
```

Expected: all new tests pass. The obsolete tests have not been invoked.

- [ ] **Step 9: Cut over code, documentation, container, and CI atomically**

Delete every file and directory listed in this task, including every subject, compatibility import, Travis configuration, publication script, encrypted key, and historical discussion. Do not preserve compatibility imports or historical tables.

Replace README with this runnable baseline documentation:

````markdown
# Python Serialization Benchmark

This project compares Python model serialization at equivalent abstraction levels. Every adapter must pass exact semantic validation before it can be timed. The active adapter registry is the source of truth; abandoned implementations are not retained.

The initial cutover provides handwritten model-to-primitive conversion and `dataclasses.asdict`. The latter is intentionally dump-only.

## Validate

```bash
uv sync --locked
uv run pytest
uv run serialization-benchmark validate
```

Python 3.12, 3.13, and 3.14 are supported. Python 3.14 is the canonical benchmark runtime.
````

Replace Docker with:

```dockerfile
FROM ghcr.io/astral-sh/uv:0.12.10 AS uv
FROM python:3.14-slim

COPY --from=uv /uv /uvx /bin/
ENV UV_COMPILE_BYTECODE=1
ENV UV_LINK_MODE=copy

WORKDIR /app
COPY pyproject.toml uv.lock README.md ./
COPY serialization_benchmark ./serialization_benchmark
COPY tests ./tests
RUN uv sync --locked

ENTRYPOINT ["uv", "run", "--locked", "serialization-benchmark"]
CMD ["validate"]
```

Initially, Compose contains only these services:

```yaml
services:
  validate:
    build: .
    command: ["validate"]
  tests:
    build: .
    entrypoint: ["uv", "run", "--locked", "pytest"]
```

Create this minimal `.github/workflows/ci.yml`:

```yaml
name: CI

on:
  push:
  pull_request:

jobs:
  correctness:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v7
      - uses: actions/setup-python@v6
        with:
          python-version: "${{ matrix.python-version }}"
      - run: uv sync --locked --python "${{ matrix.python-version }}"
      - run: uv run --python "${{ matrix.python-version }}" pytest
      - run: uv run --python "${{ matrix.python-version }}" serialization-benchmark validate
```

Regenerate `uv.lock` from `pyproject.toml`.

Run:

```bash
uv lock --check
uv run serialization-benchmark validate
uv run pytest -v
docker compose build
docker compose run --rm validate
docker compose run --rm tests
```

Expected: validation reports 2 primitive adapters and 0 encoded adapters; tests and both container services pass; no active file refers to an abandoned library or deleted script.

- [ ] **Step 10: Commit the baseline cutover**

```bash
git add -A
git commit -m "refactor: replace legacy benchmark with typed baselines"
```

---

### Task 2: Marshmallow Primitive Adapter

**Files:**
- Create: `serialization_benchmark/adapters/marshmallow.py`
- Modify: `serialization_benchmark/registry.py`
- Modify: `tests/test_registry.py`
- Modify: `pyproject.toml`
- Modify: `uv.lock`

**Interfaces:**
- Consumes: `PrimitiveAdapter`, `Parent`, `Child`, and the explicit primitive registry.
- Produces: registered slug `marshmallow` with all primitive operations.

- [ ] **Step 1: Add the dependency and failing registry assertion**

Run: `uv add marshmallow==4.3.1`.

Add `"marshmallow"` to the expected primitive slug list in `tests/test_registry.py`.

- [ ] **Step 2: Verify the test fails**

Run: `uv run pytest tests/test_registry.py -v`

Expected: the slug list is missing `marshmallow`.

- [ ] **Step 3: Implement the adapter**

Create `ChildSchema` and `ParentSchema` using `fields.Integer`, `fields.String`, `fields.Boolean`, `fields.Float`, `fields.Nested`, and `fields.List`. Use these explicit post-load constructors:

```python
class ChildSchema(Schema):
    w = fields.Integer(required=True)
    x = fields.Integer(required=True)
    y = fields.String(required=True)
    z = fields.Integer(required=True)

    @post_load
    def make_child(self, data: dict[str, object], **_: object) -> Child:
        return Child(w=int(data["w"]), x=int(data["x"]), y=str(data["y"]), z=int(data["z"]))


class ParentSchema(Schema):
    foo = fields.String(required=True)
    count = fields.Integer(required=True)
    active = fields.Boolean(required=True)
    ratio = fields.Float(required=True)
    sub = fields.Nested(ChildSchema(), required=True)
    subs = fields.List(fields.Nested(ChildSchema()), required=True)

    @post_load
    def make_parent(self, data: dict[str, object], **_: object) -> Parent:
        sub = data["sub"]
        subs = data["subs"]
        if not isinstance(sub, Child) or not isinstance(subs, list):
            raise TypeError("marshmallow returned invalid nested values")
        if not all(isinstance(child, Child) for child in subs):
            raise TypeError("marshmallow returned an invalid child list")
        return Parent(
            foo=str(data["foo"]),
            count=int(data["count"]),
            active=bool(data["active"]),
            ratio=float(data["ratio"]),
            sub=sub,
            subs=subs,
        )
```

Construct one `ParentSchema()` and one `ParentSchema(many=True)` in `MarshmallowAdapter.__init__`.

The timed methods must call the prepared schemas directly:

```python
def dump_one(self, value: object) -> JsonObject:
    return cast(JsonObject, self._one.dump(value))

def load_one(self, value: JsonObject) -> object:
    return self._one.load(value)

def dump_many(self, values: Sequence[object]) -> list[JsonObject]:
    return cast(list[JsonObject], self._many.dump(values))

def load_many(self, values: Sequence[JsonObject]) -> list[object]:
    return cast(list[object], self._many.load(values))
```

Set metadata from `importlib.metadata.version("marshmallow")` and normalize with `normalize_parent`. Register one instance after the baselines.

- [ ] **Step 4: Run contract tests**

Run:

```bash
uv run pytest tests/test_registry.py tests/test_primitive_adapters.py -v
uv run serialization-benchmark validate
```

Expected: Marshmallow passes all four exact semantic checks.

- [ ] **Step 5: Commit**

```bash
git add pyproject.toml uv.lock serialization_benchmark/adapters/marshmallow.py serialization_benchmark/registry.py tests
git commit -m "feat: benchmark marshmallow primitive conversion"
```

---

### Task 3: Django REST Framework Primitive Adapter

**Files:**
- Create: `serialization_benchmark/adapters/drf.py`
- Modify: `serialization_benchmark/registry.py`
- Modify: `tests/test_registry.py`
- Modify: `pyproject.toml`
- Modify: `uv.lock`

**Interfaces:**
- Consumes: the primitive adapter contract and canonical dataclasses.
- Produces: registered slug `drf` with all primitive operations.

- [ ] **Step 1: Add dependencies and a failing registry assertion**

Run:

```bash
uv add django==6.1.1 djangorestframework==3.18.0
```

Add `"drf"` to the expected primitive slug list.

- [ ] **Step 2: Verify the test fails**

Run: `uv run pytest tests/test_registry.py -v`

Expected: the slug list is missing `drf`.

- [ ] **Step 3: Implement real DRF serialization and model creation**

Define nested `serializers.Serializer` classes with declared fields. `ParentSerializer.create` must convert the validated nested dictionaries into `Child` and `Parent` objects. Do not bypass DRF validation on load:

```python
class ChildSerializer(serializers.Serializer):
    w = serializers.IntegerField()
    x = serializers.IntegerField()
    y = serializers.CharField()
    z = serializers.IntegerField()


class ParentSerializer(serializers.Serializer):
    foo = serializers.CharField()
    count = serializers.IntegerField()
    active = serializers.BooleanField()
    ratio = serializers.FloatField()
    sub = ChildSerializer()
    subs = ChildSerializer(many=True)

    def create(self, validated_data: dict[str, object]) -> Parent:
        raw_sub = cast(dict[str, object], validated_data["sub"])
        raw_subs = cast(list[dict[str, object]], validated_data["subs"])

        def make_child(item: dict[str, object]) -> Child:
            return Child(
                w=int(item["w"]),
                x=int(item["x"]),
                y=str(item["y"]),
                z=int(item["z"]),
            )

        return Parent(
            foo=str(validated_data["foo"]),
            count=int(validated_data["count"]),
            active=bool(validated_data["active"]),
            ratio=float(validated_data["ratio"]),
            sub=make_child(raw_sub),
            subs=[make_child(item) for item in raw_subs],
        )
```

```python
def load_one(self, value: JsonObject) -> object:
    serializer = ParentSerializer(data=value)
    serializer.is_valid(raise_exception=True)
    return serializer.save()

def load_many(self, values: Sequence[JsonObject]) -> list[object]:
    serializer = ParentSerializer(data=values, many=True)
    serializer.is_valid(raise_exception=True)
    return cast(list[object], serializer.save())

def dump_one(self, value: object) -> JsonObject:
    return cast(JsonObject, ParentSerializer(value).data)

def dump_many(self, values: Sequence[object]) -> list[JsonObject]:
    return cast(list[JsonObject], ParentSerializer(values, many=True).data)
```

Use `version("djangorestframework")` in metadata and register the adapter after Marshmallow.

- [ ] **Step 4: Run contract tests**

Run:

```bash
uv run pytest tests/test_registry.py tests/test_primitive_adapters.py -v
uv run serialization-benchmark validate
```

Expected: DRF validates exact single and batch round trips.

- [ ] **Step 5: Commit**

```bash
git add pyproject.toml uv.lock serialization_benchmark/adapters/drf.py serialization_benchmark/registry.py tests/test_registry.py
git commit -m "feat: benchmark drf primitive conversion"
```

---

### Task 4: cattrs Primitive Adapter

**Files:**
- Create: `serialization_benchmark/adapters/cattrs.py`
- Modify: `serialization_benchmark/registry.py`
- Modify: `tests/test_registry.py`
- Modify: `pyproject.toml`
- Modify: `uv.lock`

**Interfaces:**
- Produces: registered slug `cattrs` with generated hooks for all primitive operations.

- [ ] **Step 1: Add cattrs and a failing registry assertion**

Run: `uv add cattrs==26.2.0`.

Add `"cattrs"` to the expected primitive slug list.

- [ ] **Step 2: Verify failure**

Run: `uv run pytest tests/test_registry.py -v`

Expected: `cattrs` is absent.

- [ ] **Step 3: Implement the adapter with cached hooks**

Construct a `cattrs.Converter` once and retrieve hooks before timing:

```python
class CattrsAdapter:
    metadata = AdapterMetadata(
        slug="cattrs",
        name="cattrs",
        package="cattrs",
        version=version("cattrs"),
    )
    operations = ALL_PRIMITIVE_OPERATIONS

    def __init__(self) -> None:
        converter = Converter()
        self._dump_one = converter.get_unstructure_hook(Parent)
        self._load_one = converter.get_structure_hook(Parent)
        self._dump_many = converter.get_unstructure_hook(list[Parent])
        self._load_many = converter.get_structure_hook(list[Parent])

    def dump_one(self, value: object) -> JsonObject:
        return cast(JsonObject, self._dump_one(value))

    def load_one(self, value: JsonObject) -> object:
        return self._load_one(value, Parent)

    def dump_many(self, values: Sequence[object]) -> list[JsonObject]:
        return cast(list[JsonObject], self._dump_many(values))

    def load_many(self, values: Sequence[JsonObject]) -> list[object]:
        return cast(list[object], self._load_many(values, list[Parent]))
```

The hook signatures above are verified against cattrs 26.2.0. Register the adapter and do not rebuild a converter inside timed methods.

- [ ] **Step 4: Run validation**

Run: `uv run pytest tests/test_registry.py tests/test_primitive_adapters.py -v`

Expected: cattrs passes all contract cases and initialization occurs once during registry construction.

- [ ] **Step 5: Commit**

```bash
git add pyproject.toml uv.lock serialization_benchmark/adapters/cattrs.py serialization_benchmark/registry.py tests/test_registry.py
git commit -m "feat: benchmark cattrs primitive conversion"
```

---

### Task 5: mashumaro Primitive Adapter

**Files:**
- Create: `serialization_benchmark/adapters/mashumaro.py`
- Modify: `serialization_benchmark/registry.py`
- Modify: `tests/test_registry.py`
- Modify: `pyproject.toml`
- Modify: `uv.lock`

**Interfaces:**
- Produces: registered slug `mashumaro` backed by prebuilt basic encoders and decoders.

- [ ] **Step 1: Add mashumaro and a failing assertion**

Run: `uv add mashumaro==3.22`.

Add `"mashumaro"` to the expected primitive slug list.

- [ ] **Step 2: Verify failure**

Run: `uv run pytest tests/test_registry.py -v`

Expected: `mashumaro` is absent.

- [ ] **Step 3: Implement pre-generated codecs**

```python
class MashumaroAdapter:
    metadata = AdapterMetadata(
        slug="mashumaro",
        name="mashumaro",
        package="mashumaro",
        version=version("mashumaro"),
    )
    operations = ALL_PRIMITIVE_OPERATIONS

    def __init__(self) -> None:
        self._one_encoder = BasicEncoder(Parent)
        self._one_decoder = BasicDecoder(Parent)
        self._many_encoder = BasicEncoder(list[Parent])
        self._many_decoder = BasicDecoder(list[Parent])

    def dump_one(self, value: object) -> JsonObject:
        return cast(JsonObject, self._one_encoder.encode(value))

    def load_one(self, value: JsonObject) -> object:
        return self._one_decoder.decode(value)

    def dump_many(self, values: Sequence[object]) -> list[JsonObject]:
        return cast(list[JsonObject], self._many_encoder.encode(values))

    def load_many(self, values: Sequence[JsonObject]) -> list[object]:
        return cast(list[object], self._many_decoder.decode(values))
```

Import `BasicEncoder` and `BasicDecoder` from `mashumaro.codecs`. Registry construction and the pre-benchmark validation call absorb code generation.

- [ ] **Step 4: Run validation**

Run: `uv run pytest tests/test_registry.py tests/test_primitive_adapters.py -v`

Expected: mashumaro passes all operations.

- [ ] **Step 5: Commit**

```bash
git add pyproject.toml uv.lock serialization_benchmark/adapters/mashumaro.py serialization_benchmark/registry.py tests/test_registry.py
git commit -m "feat: benchmark mashumaro primitive conversion"
```

---

### Task 6: msgspec Primitive Adapter

**Files:**
- Create: `serialization_benchmark/adapters/msgspec.py`
- Modify: `serialization_benchmark/registry.py`
- Modify: `tests/test_registry.py`
- Modify: `pyproject.toml`
- Modify: `uv.lock`

**Interfaces:**
- Produces: registered slug `msgspec` using stdlib dataclasses, not `msgspec.Struct`.

- [ ] **Step 1: Add msgspec and a failing assertion**

Run: `uv add msgspec==0.21.1`.

Add `"msgspec"` to the expected primitive slug list.

- [ ] **Step 2: Verify failure**

Run: `uv run pytest tests/test_registry.py -v`

Expected: `msgspec` is absent.

- [ ] **Step 3: Implement the shared-dataclass adapter**

```python
class MsgspecAdapter:
    metadata = AdapterMetadata(
        slug="msgspec",
        name="msgspec",
        package="msgspec",
        version=version("msgspec"),
    )
    operations = ALL_PRIMITIVE_OPERATIONS

    def dump_one(self, value: object) -> JsonObject:
        return cast(JsonObject, msgspec.to_builtins(value))

    def load_one(self, value: JsonObject) -> object:
        return msgspec.convert(value, type=Parent, strict=True)

    def dump_many(self, values: Sequence[object]) -> list[JsonObject]:
        return cast(list[JsonObject], msgspec.to_builtins(values))

    def load_many(self, values: Sequence[JsonObject]) -> list[object]:
        return cast(list[object], msgspec.convert(values, type=list[Parent], strict=True))
```

Normalize through `normalize_parent` and register the adapter. Do not add a Struct model in this plan.

- [ ] **Step 4: Run validation**

Run: `uv run pytest tests/test_registry.py tests/test_primitive_adapters.py -v`

Expected: msgspec passes all shared-dataclass cases.

- [ ] **Step 5: Commit**

```bash
git add pyproject.toml uv.lock serialization_benchmark/adapters/msgspec.py serialization_benchmark/registry.py tests/test_registry.py
git commit -m "feat: benchmark msgspec primitive conversion"
```

---

### Task 7: Pydantic v2 Primitive Adapter

**Files:**
- Create: `serialization_benchmark/adapters/pydantic.py`
- Modify: `serialization_benchmark/registry.py`
- Modify: `tests/test_registry.py`
- Modify: `pyproject.toml`
- Modify: `uv.lock`

**Interfaces:**
- Produces: registered slug `pydantic` using cached `TypeAdapter` instances over stdlib dataclasses.

- [ ] **Step 1: Add Pydantic and a failing assertion**

Run: `uv add pydantic==2.13.5`.

Add `"pydantic"` to the expected primitive slug list.

- [ ] **Step 2: Verify failure**

Run: `uv run pytest tests/test_registry.py -v`

Expected: `pydantic` is absent.

- [ ] **Step 3: Implement cached TypeAdapters**

```python
class PydanticAdapter:
    metadata = AdapterMetadata(
        slug="pydantic",
        name="Pydantic v2",
        package="pydantic",
        version=version("pydantic"),
    )
    operations = ALL_PRIMITIVE_OPERATIONS

    def __init__(self) -> None:
        self._one = TypeAdapter(Parent)
        self._many = TypeAdapter(list[Parent])

    def dump_one(self, value: object) -> JsonObject:
        return cast(JsonObject, self._one.dump_python(value, mode="json"))

    def load_one(self, value: JsonObject) -> object:
        return self._one.validate_python(value)

    def dump_many(self, values: Sequence[object]) -> list[JsonObject]:
        return cast(list[JsonObject], self._many.dump_python(values, mode="json"))

    def load_many(self, values: Sequence[JsonObject]) -> list[object]:
        return cast(list[object], self._many.validate_python(values))
```

Register the adapter and keep `BaseModel` out of the primary suite.

- [ ] **Step 4: Run validation**

Run: `uv run pytest tests/test_registry.py tests/test_primitive_adapters.py -v`

Expected: Pydantic validates the stdlib dataclass model exactly.

- [ ] **Step 5: Commit**

```bash
git add pyproject.toml uv.lock serialization_benchmark/adapters/pydantic.py serialization_benchmark/registry.py tests/test_registry.py
git commit -m "feat: benchmark pydantic primitive conversion"
```

---

### Task 8: serpyco-rs Primitive Adapter

**Files:**
- Create: `serialization_benchmark/adapters/serpyco_rs.py`
- Modify: `serialization_benchmark/registry.py`
- Modify: `tests/test_registry.py`
- Modify: `pyproject.toml`
- Modify: `uv.lock`

**Interfaces:**
- Produces: registered slug `serpyco-rs` using prepared single and list serializers.

- [ ] **Step 1: Add serpyco-rs and a failing assertion**

Run: `uv add serpyco-rs==1.22.0`.

Add `"serpyco-rs"` to the expected primitive slug list.

- [ ] **Step 2: Verify failure**

Run: `uv run pytest tests/test_registry.py -v`

Expected: `serpyco-rs` is absent.

- [ ] **Step 3: Implement the adapter**

```python
class SerpycoRsAdapter:
    metadata = AdapterMetadata(
        slug="serpyco-rs",
        name="serpyco-rs",
        package="serpyco-rs",
        version=version("serpyco-rs"),
    )
    operations = ALL_PRIMITIVE_OPERATIONS

    def __init__(self) -> None:
        self._one = Serializer(Parent)
        self._many = Serializer(list[Parent])

    def dump_one(self, value: object) -> JsonObject:
        return cast(JsonObject, self._one.dump(value))

    def load_one(self, value: JsonObject) -> object:
        return self._one.load(value)

    def dump_many(self, values: Sequence[object]) -> list[JsonObject]:
        return cast(list[JsonObject], self._many.dump(values))

    def load_many(self, values: Sequence[JsonObject]) -> list[object]:
        return cast(list[object], self._many.load(values))
```

Import `Serializer` from `serpyco_rs` and register the adapter.

- [ ] **Step 4: Run validation**

Run: `uv run pytest tests/test_registry.py tests/test_primitive_adapters.py -v`

Expected: serpyco-rs passes all primitive operations.

- [ ] **Step 5: Commit**

```bash
git add pyproject.toml uv.lock serialization_benchmark/adapters/serpyco_rs.py serialization_benchmark/registry.py tests/test_registry.py
git commit -m "feat: benchmark serpyco-rs primitive conversion"
```

---

### Task 9: Adaptix Primitive Adapter

**Files:**
- Create: `serialization_benchmark/adapters/adaptix.py`
- Modify: `serialization_benchmark/registry.py`
- Modify: `tests/test_registry.py`
- Modify: `pyproject.toml`
- Modify: `uv.lock`

**Interfaces:**
- Produces: registered slug `adaptix` with `prerelease=True`.

- [ ] **Step 1: Add the pinned beta and a failing assertion**

Run: `uv add adaptix==3.0.0b12`.

Add `"adaptix"` to the expected primitive slug list.

- [ ] **Step 2: Verify failure**

Run: `uv run pytest tests/test_registry.py -v`

Expected: `adaptix` is absent.

- [ ] **Step 3: Implement one cached Retort**

```python
class AdaptixAdapter:
    metadata = AdapterMetadata(
        slug="adaptix",
        name="Adaptix 3.0 beta",
        package="adaptix",
        version=version("adaptix"),
        prerelease=True,
    )
    operations = ALL_PRIMITIVE_OPERATIONS

    def __init__(self) -> None:
        self._retort = Retort()

    def dump_one(self, value: object) -> JsonObject:
        return cast(JsonObject, self._retort.dump(value, Parent))

    def load_one(self, value: JsonObject) -> object:
        return self._retort.load(value, Parent)

    def dump_many(self, values: Sequence[object]) -> list[JsonObject]:
        return cast(list[JsonObject], self._retort.dump(values, list[Parent]))

    def load_many(self, values: Sequence[JsonObject]) -> list[object]:
        return cast(list[object], self._retort.load(values, list[Parent]))
```

The registry-level instance and mandatory pre-run validation ensure Adaptix compiles and caches loaders before timing.

- [ ] **Step 4: Run validation**

Run:

```bash
uv run pytest tests/test_registry.py tests/test_primitive_adapters.py -v
uv run serialization-benchmark validate
```

Expected: 10 primitive adapters validate, including the two baselines.

- [ ] **Step 5: Commit**

```bash
git add pyproject.toml uv.lock serialization_benchmark/adapters/adaptix.py serialization_benchmark/registry.py tests/test_registry.py
git commit -m "feat: benchmark adaptix primitive conversion"
```

---

### Task 10: Calibrated pyperf Runner

**Files:**
- Create: `serialization_benchmark/runner.py`
- Create: `tests/test_runner.py`
- Modify: `serialization_benchmark/cli.py`
- Modify: `tests/test_cli.py`
- Modify: `pyproject.toml`
- Modify: `uv.lock`

**Interfaces:**
- Consumes: adapter registries and `FixtureSet`.
- Produces: `BenchmarkCase`, `build_primitive_cases`, `build_encoded_cases`, and module-executable pyperf runner.
- Produces: `serialization-benchmark run {primitive,encoded} --output PATH` with adapter and operation filters.

- [ ] **Step 1: Add pyperf and failing case-construction tests**

Run: `uv add pyperf==2.10.0`.

Create `tests/test_runner.py`:

```python
from serialization_benchmark.fixtures import make_fixtures
from serialization_benchmark.runner import build_primitive_cases


def test_primitive_cases_are_partitioned_by_operation() -> None:
    cases = build_primitive_cases(make_fixtures())
    names = {case.name for case in cases}

    assert "primitive.dump_one.handwritten.dataclass" in names
    assert "primitive.load_one.handwritten.dataclass" in names
    assert "primitive.dump_many.dataclasses-asdict.dataclass" in names
    assert "primitive.load_one.dataclasses-asdict.dataclass" not in names
    assert all(case.metadata["gc"] == "enabled" for case in cases)
```

Add an end-to-end CLI test that runs only handwritten `dump_one` with `--processes=1 --values=1 --warmups=1 --loops=1` and asserts the output JSON exists.

- [ ] **Step 2: Verify failure**

Run: `uv run pytest tests/test_runner.py tests/test_cli.py -v`

Expected: `runner` and the `run` CLI command are missing.

- [ ] **Step 3: Implement cases without timing setup**

Define:

```python
@dataclass(frozen=True)
class BenchmarkCase:
    name: str
    operation: Callable[[], object]
    metadata: dict[str, str | int]


def _case_metadata(
    adapter: PrimitiveAdapter | EncodedAdapter,
    *,
    tier: str,
    operation: str,
    batch_size: int,
    encoded_format: str = "",
) -> dict[str, str | int]:
    metadata: dict[str, str | int] = {
        "tier": tier,
        "adapter_slug": adapter.metadata.slug,
        "adapter_name": adapter.metadata.name,
        "package": adapter.metadata.package,
        "library_version": adapter.metadata.version,
        "model_strategy": adapter.metadata.model_strategy,
        "prerelease": str(adapter.metadata.prerelease).lower(),
        "operation": operation,
        "batch_size": batch_size,
        "gc": "enabled",
    }
    if encoded_format:
        metadata["format"] = encoded_format
    return metadata
```

Use `functools.partial` to bind prepared `one`, `list(many)`, `one_primitive`, and `list(many_primitives)` outside timing. Build cases only for declared operations.

For encoded decode cases, call `encode_one` and `encode_many` once while building cases, then bind those bytes. This preparation must not occur inside `operation`.

- [ ] **Step 4: Implement pyperf execution**

```python
def _time_operation(loops: int, operation: Callable[[], object]) -> float:
    gc.enable()
    result: object = None
    start = time.perf_counter()
    for _ in range(loops):
        result = operation()
    elapsed = time.perf_counter() - start
    if result is _UNREACHABLE_SENTINEL:
        raise AssertionError("unreachable")
    return elapsed
```

Create exactly one `pyperf.Runner` per process. Add `--tier`, repeatable `--adapter`, and repeatable `--operation` to its parser and propagate them through `add_cmdline_args`. Before `bench_time_func` calls:

1. build fixtures;
2. validate all selected adapters;
3. prepare cases and decode inputs;
4. filter cases;
5. call `runner.bench_time_func(case.name, _time_operation, case.operation, metadata=case.metadata)`.

Add global metadata for git revision, benchmark package version, GC policy, and `run_utc=datetime.now(timezone.utc).isoformat()`. Keep `batch_size` in case metadata because single cases use 1 and batch cases use 100. Let pyperf collect interpreter, CPU, host, platform, timer, and its own timestamps.

- [ ] **Step 5: Wire the CLI through a real subprocess**

The `run` command invokes:

```python
command = [
    sys.executable,
    "-m",
    "serialization_benchmark.runner",
    "--tier",
    args.tier,
    "-o",
    str(args.output),
]
```

Append each filter, then append pyperf arguments found after `--`. Use `subprocess.run(command, check=False)` and return its return code. This keeps pyperf worker re-execution isolated from the outer CLI parser.

- [ ] **Step 6: Run tests and inspect raw metadata**

Run:

```bash
uv run pytest tests/test_runner.py tests/test_cli.py -v
benchmark_smoke_dir=$(mktemp -d /tmp/serialization-benchmark.XXXXXX)
uv run serialization-benchmark run primitive --output "$benchmark_smoke_dir/primitive.json" --adapter handwritten --operation dump_one -- --processes=1 --values=1 --warmups=1 --loops=1
uv run python -m pyperf metadata "$benchmark_smoke_dir/primitive.json"
```

Expected: the smoke file contains `tier=primitive`, `operation=dump_one`, `batch_size=1`, `gc=enabled`, adapter version, git revision, and pyperf system metadata.

- [ ] **Step 7: Commit**

```bash
git add pyproject.toml uv.lock serialization_benchmark/runner.py serialization_benchmark/cli.py tests/test_runner.py tests/test_cli.py
git commit -m "feat: add calibrated pyperf benchmark runner"
```

---

### Task 11: JSON Encoded Adapters

**Files:**
- Create: `serialization_benchmark/adapters/encoded_json.py`
- Create: `tests/test_encoded_adapters.py`
- Modify: `serialization_benchmark/registry.py`
- Modify: `serialization_benchmark/validation.py`
- Modify: `serialization_benchmark/runner.py`
- Modify: `tests/test_registry.py`
- Modify: `pyproject.toml`
- Modify: `uv.lock`

**Interfaces:**
- Consumes: `EncodedAdapter` and cached codecs already provided by maintained dependencies.
- Produces: JSON slugs `stdlib-json`, `orjson`, `msgspec-json`, `mashumaro-json`, `pydantic-json`, and `serpyco-rs-json`.
- Produces: `validate_encoded_adapter(adapter, fixtures)`.

- [ ] **Step 1: Add orjson and failing encoded contract tests**

Run: `uv add orjson==3.12.0`.

Create `tests/test_encoded_adapters.py`:

```python
import pytest

from serialization_benchmark.contracts import EncodedAdapter
from serialization_benchmark.fixtures import make_fixtures
from serialization_benchmark.registry import encoded_adapters
from serialization_benchmark.validation import validate_encoded_adapter


@pytest.mark.parametrize(
    "adapter",
    encoded_adapters(),
    ids=lambda adapter: adapter.metadata.slug,
)
def test_registered_encoded_adapter_is_semantically_correct(
    adapter: EncodedAdapter,
) -> None:
    validate_encoded_adapter(adapter, make_fixtures())


def test_json_adapters_are_registered() -> None:
    assert [
        adapter.metadata.slug
        for adapter in encoded_adapters()
        if adapter.format == "json"
    ] == [
        "stdlib-json",
        "orjson",
        "msgspec-json",
        "mashumaro-json",
        "pydantic-json",
        "serpyco-rs-json",
    ]
```

- [ ] **Step 2: Verify failure**

Run: `uv run pytest tests/test_encoded_adapters.py -v`

Expected: the JSON slug assertion fails with an empty list.

- [ ] **Step 3: Implement exact encoded validation**

`validate_encoded_adapter` checks every declared operation:

```python
encoded_one = adapter.encode_one(fixtures.one)
if type(encoded_one) is not bytes:
    raise AdapterValidationError(f"{slug}.encode_one returned {type(encoded_one).__name__}")
decoded_one = adapter.normalize(adapter.decode_one(encoded_one))
_assert_equal(f"{slug}.decode_one", fixtures.one, decoded_one)

encoded_many = adapter.encode_many(list(fixtures.many))
if type(encoded_many) is not bytes:
    raise AdapterValidationError(f"{slug}.encode_many returned {type(encoded_many).__name__}")
decoded_many = [adapter.normalize(value) for value in adapter.decode_many(encoded_many)]
_assert_equal(f"{slug}.decode_many", list(fixtures.many), decoded_many)
```

Call encode twice and assert equal byte lengths. Store payload sizes in case metadata as `payload_bytes` when `build_encoded_cases` prepares decode input.

- [ ] **Step 4: Implement the six JSON adapters**

All adapters declare `format = "json"` and all encoded operations.

- `StdlibJsonAdapter` uses compact separators. Encode `parent_to_primitive` or a list of those, then `json.dumps(..., separators=(",", ":")).encode("utf-8")`. Decode with `json.loads` followed by `parent_from_primitive`.
- `OrjsonAdapter` calls `orjson.dumps` directly on dataclasses and lists. Decode with `orjson.loads` followed by `parent_from_primitive`.
- `MsgspecJsonAdapter` constructs `msgspec.json.Encoder()`, `Decoder(Parent)`, and `Decoder(list[Parent])` once.
- `MashumaroJsonAdapter` constructs `mashumaro.codecs.json.JSONEncoder` and `JSONDecoder` for `Parent` and `list[Parent]` once. Convert encoder strings to UTF-8 bytes and decode bytes to strings before the decoder call.
- `PydanticJsonAdapter` constructs `TypeAdapter(Parent)` and `TypeAdapter(list[Parent])` once, calls `dump_json` and `validate_json`.
- `SerpycoRsJsonAdapter` constructs `Serializer(Parent, codec=JSON)` and `Serializer(list[Parent], codec=JSON)` once.

The msgspec pattern is:

```python
def encode_one(self, value: object) -> bytes:
    return self._encoder.encode(value)

def decode_one(self, value: bytes) -> object:
    return self._one_decoder.decode(value)

def encode_many(self, values: Sequence[object]) -> bytes:
    return self._encoder.encode(values)

def decode_many(self, value: bytes) -> list[object]:
    return cast(list[object], self._many_decoder.decode(value))
```

Register the six adapters in the listed order.

- [ ] **Step 5: Run exact round trips and runner cases**

Run:

```bash
uv run pytest tests/test_encoded_adapters.py tests/test_runner.py tests/test_registry.py -v
uv run serialization-benchmark validate
```

Expected: all JSON adapters return bytes, round-trip one and 100 parents exactly, and report stable payload lengths.

- [ ] **Step 6: Commit**

```bash
git add pyproject.toml uv.lock serialization_benchmark/adapters/encoded_json.py serialization_benchmark/registry.py serialization_benchmark/validation.py serialization_benchmark/runner.py tests
git commit -m "feat: benchmark json byte serialization"
```

---

### Task 12: MessagePack Encoded Adapters

**Files:**
- Create: `serialization_benchmark/adapters/encoded_msgpack.py`
- Modify: `serialization_benchmark/registry.py`
- Modify: `tests/test_encoded_adapters.py`
- Modify: `pyproject.toml`
- Modify: `uv.lock`

**Interfaces:**
- Produces: MessagePack slugs `msgspec-msgpack`, `mashumaro-msgpack`, `serpyco-rs-msgpack`, and `ormsgpack`.

- [ ] **Step 1: Add dependencies and a failing slug assertion**

Run:

```bash
uv add msgpack==1.2.2 ormsgpack==1.12.2
```

Add a MessagePack registry assertion with the four exact slugs.

- [ ] **Step 2: Verify failure**

Run: `uv run pytest tests/test_encoded_adapters.py -v`

Expected: no MessagePack adapters are registered.

- [ ] **Step 3: Implement MessagePack adapters**

- `MsgspecMessagePackAdapter` uses one `msgspec.msgpack.Encoder` plus typed `Decoder(Parent)` and `Decoder(list[Parent])`.
- `MashumaroMessagePackAdapter` uses `MessagePackEncoder` and `MessagePackDecoder` from `mashumaro.codecs.msgpack` for `Parent` and `list[Parent]`.
- `SerpycoRsMessagePackAdapter` uses bound `MSGPACK` codecs on single and list serializers.
- `OrmsgpackAdapter` calls `ormsgpack.packb` directly on dataclasses and lists. `unpackb` output is converted through `parent_from_primitive`.

The ormsgpack decode boundary must be narrowed once:

```python
def decode_one(self, value: bytes) -> object:
    return parent_from_primitive(cast(JsonObject, ormsgpack.unpackb(value)))

def decode_many(self, value: bytes) -> list[object]:
    raw = cast(list[JsonObject], ormsgpack.unpackb(value))
    return [parent_from_primitive(item) for item in raw]
```

Do not enable sorting or passthrough options; the canonical model uses supported dataclass fields and correctness compares semantics rather than byte order.

- [ ] **Step 4: Run validation**

Run:

```bash
uv run pytest tests/test_encoded_adapters.py tests/test_runner.py -v
uv run serialization-benchmark validate
```

Expected: all four MessagePack implementations round-trip exact models and remain partitioned from JSON.

- [ ] **Step 5: Commit**

```bash
git add pyproject.toml uv.lock serialization_benchmark/adapters/encoded_msgpack.py serialization_benchmark/registry.py tests/test_encoded_adapters.py
git commit -m "feat: benchmark messagepack byte serialization"
```

---

### Task 13: Avro and Pickle Encoded Adapters

**Files:**
- Create: `serialization_benchmark/adapters/encoded_avro.py`
- Create: `serialization_benchmark/adapters/encoded_pickle.py`
- Modify: `serialization_benchmark/registry.py`
- Modify: `tests/test_encoded_adapters.py`
- Modify: `pyproject.toml`
- Modify: `uv.lock`

**Interfaces:**
- Produces: encoded slugs `avro` and `pickle` in distinct format partitions.

- [ ] **Step 1: Add Avro and failing format assertions**

Run: `uv add avro==1.12.2`.

Assert that the Avro partition is `["avro"]` and the pickle partition is `["pickle"]`.

- [ ] **Step 2: Verify failure**

Run: `uv run pytest tests/test_encoded_adapters.py -v`

Expected: both partitions are empty.

- [ ] **Step 3: Implement Avro schemas and adapter**

Define and parse these schema objects, then create `DatumWriter` and `DatumReader` instances in `__init__`:

```python
CHILD_SCHEMA: dict[str, object] = {
    "type": "record",
    "name": "Child",
    "fields": [
        {"name": "w", "type": "long"},
        {"name": "x", "type": "long"},
        {"name": "y", "type": "string"},
        {"name": "z", "type": "long"},
    ],
}
PARENT_SCHEMA: dict[str, object] = {
    "type": "record",
    "name": "Parent",
    "fields": [
        {"name": "foo", "type": "string"},
        {"name": "count", "type": "long"},
        {"name": "active", "type": "boolean"},
        {"name": "ratio", "type": "double"},
        {"name": "sub", "type": CHILD_SCHEMA},
        {"name": "subs", "type": {"type": "array", "items": "Child"}},
    ],
}
BATCH_SCHEMA: dict[str, object] = {"type": "array", "items": PARENT_SCHEMA}
```

Encode with a new `io.BytesIO` and `BinaryEncoder` per operation:

```python
def _write(self, writer: DatumWriter, value: object) -> bytes:
    buffer = BytesIO()
    writer.write(value, BinaryEncoder(buffer))
    return buffer.getvalue()

def encode_one(self, value: object) -> bytes:
    return self._write(self._one_writer, parent_to_primitive(cast(Parent, value)))

def decode_one(self, value: bytes) -> object:
    raw = self._one_reader.read(BinaryDecoder(BytesIO(value)))
    return parent_from_primitive(cast(JsonObject, raw))
```

The batch methods map all parents to primitives before writing and reconstruct all parents after reading.

- [ ] **Step 4: Implement pickle adapter**

Use `pickle.dumps(value, protocol=pickle.HIGHEST_PROTOCOL)` for one and batch encoding, `pickle.loads` for decoding, and runtime type checks in `normalize`. Do not accept untrusted pickle data anywhere in the CLI.

- [ ] **Step 5: Run validation**

Run:

```bash
uv run pytest tests/test_encoded_adapters.py tests/test_runner.py -v
uv run serialization-benchmark validate
```

Expected: Avro and pickle pass exact round trips and appear only in their own format partitions.

- [ ] **Step 6: Commit**

```bash
git add pyproject.toml uv.lock serialization_benchmark/adapters/encoded_avro.py serialization_benchmark/adapters/encoded_pickle.py serialization_benchmark/registry.py tests/test_encoded_adapters.py
git commit -m "feat: benchmark avro and pickle byte serialization"
```

---

### Task 14: Raw Result Reporting

**Files:**
- Create: `serialization_benchmark/reporting.py`
- Create: `serialization_benchmark/report.css`
- Create: `tests/fixtures/pyperf-smoke.json`
- Create: `tests/test_reporting.py`
- Modify: `serialization_benchmark/cli.py`
- Modify: `tests/test_cli.py`

**Interfaces:**
- Produces: `ResultRow`, `BenchmarkReport`, `load_report(path: Path) -> BenchmarkReport`, `render_markdown`, and `render_html`.
- Produces: `serialization-benchmark report RAW_JSON --markdown PATH --html PATH`.

- [ ] **Step 1: Add a deterministic raw fixture and failing report tests**

Create `tests/fixtures/pyperf-smoke.json` with this compact pyperf 1.0 suite:

```json
{"benchmarks":[{"metadata":{"adapter_name":"Handwritten","adapter_slug":"handwritten","name":"primitive.dump_one.handwritten.dataclass","operation":"dump_one","tier":"primitive"},"runs":[{"values":[0.000001,0.0000011]}]},{"metadata":{"adapter_name":"stdlib json","adapter_slug":"stdlib-json","format":"json","name":"encoded.json.encode_one.stdlib-json.dataclass","operation":"encode_one","payload_bytes":256,"tier":"encoded"},"runs":[{"values":[0.000002,0.0000022]}]}],"metadata":{"batch_size":1,"benchmark_version":"0.1.0","cpu_count":8,"gc":"enabled","git_revision":"fixture-revision","library_version":"3.14.0","model_strategy":"dataclass","package":"python","perf_version":"2.10.0","platform":"fixture-platform","prerelease":"false","python_implementation":"cpython","python_version":"3.14.0 (64-bit)","run_utc":"2026-09-06T21:00:00+00:00","unit":"second"},"version":"1.0"}
```

Create `tests/test_reporting.py`:

```python
from pathlib import Path

from serialization_benchmark.reporting import load_report, render_html, render_markdown


FIXTURE = Path("tests/fixtures/pyperf-smoke.json")


def test_results_preserve_partitions_and_payload_size() -> None:
    report = load_report(FIXTURE)
    rows = report.rows

    assert [(row.tier, row.format, row.operation) for row in rows] == [
        ("primitive", "", "dump_one"),
        ("encoded", "json", "encode_one"),
    ]
    assert rows[1].payload_bytes == 256
    assert rows[0].samples == 2
    assert report.metadata["git_revision"] == "fixture-revision"


def test_renderers_do_not_create_a_composite_score() -> None:
    report = load_report(FIXTURE)
    markdown = render_markdown(report)
    html = render_html(report)

    assert "Primitive / dump_one / batch 1" in markdown
    assert "Encoded / JSON / encode_one / batch 1" in markdown
    assert "fixture-platform" in markdown
    assert "Composite" not in markdown
    assert "<table" in html
    assert "256 B" in html
```

- [ ] **Step 2: Verify failure**

Run: `uv run pytest tests/test_reporting.py -v`

Expected: `serialization_benchmark.reporting` is missing.

- [ ] **Step 3: Implement typed pyperf parsing**

```python
@dataclass(frozen=True)
class ResultRow:
    tier: str
    format: str
    operation: str
    batch_size: int
    adapter_slug: str
    adapter_name: str
    library_version: str
    model_strategy: str
    prerelease: bool
    mean_seconds: float
    stdev_seconds: float
    operations_per_second: float
    samples: int
    payload_bytes: int | None


type MetadataValue = str | int | float | bool


@dataclass(frozen=True)
class BenchmarkReport:
    rows: tuple[ResultRow, ...]
    metadata: dict[str, MetadataValue]


def load_report(path: Path) -> BenchmarkReport:
    suite = pyperf.BenchmarkSuite.load(str(path))
    rows: list[ResultRow] = []
    for benchmark in suite.get_benchmarks():
        metadata = benchmark.get_metadata()
        mean = benchmark.mean()
        rows.append(
            ResultRow(
                tier=_required_str(metadata, "tier"),
                format=_optional_str(metadata, "format") or "",
                operation=_required_str(metadata, "operation"),
                batch_size=_required_int(metadata, "batch_size"),
                adapter_slug=_required_str(metadata, "adapter_slug"),
                adapter_name=_required_str(metadata, "adapter_name"),
                library_version=_required_str(metadata, "library_version"),
                model_strategy=_required_str(metadata, "model_strategy"),
                prerelease=_required_str(metadata, "prerelease") == "true",
                mean_seconds=mean,
                stdev_seconds=benchmark.stdev() if benchmark.get_nvalue() > 1 else 0.0,
                operations_per_second=1.0 / mean,
                samples=benchmark.get_nvalue(),
                payload_bytes=_optional_int(metadata, "payload_bytes"),
            )
        )
    metadata = _narrow_metadata(suite.get_metadata())
    return BenchmarkReport(
        rows=tuple(sorted(rows, key=_result_sort_key)),
        metadata=metadata,
    )
```

Required metadata helpers raise `ReportError` naming the benchmark and missing or mistyped key. `_narrow_metadata` accepts only string, integer, float, and boolean values, converting other pyperf metadata values to their formatted string representation with `pyperf.format_metadata`.

- [ ] **Step 4: Implement partitioned Markdown and HTML**

Group by `(tier, format, operation, batch_size, model_strategy)`. Render one table per group with version, mean, standard deviation, operations/second, samples, prerelease marker, and payload bytes for encoded groups.

Before the tables, render Python version and implementation, platform, CPU model when available, CPU count, git revision, benchmark version, GC policy, and `run_utc` from `BenchmarkReport.metadata`. Missing optional CPU model data renders as `Unknown`; missing Python, platform, git, benchmark version, GC, or UTC timestamp metadata raises `ReportError`.

Only primitive groups containing `handwritten` and JSON groups containing `stdlib-json` receive a relative column. Compute `row.mean_seconds / baseline.mean_seconds` within that group. MessagePack, Avro, and pickle groups have no relative column.

Format seconds as ns below 1 µs, µs below 1 ms, ms below 1 second, and seconds otherwise. Escape every HTML value with `html.escape`. Load checked-in CSS with `importlib.resources.files("serialization_benchmark").joinpath("report.css").read_text()`.

Every HTML report includes a navigation block with links to `index.html` labeled `Primitive results` and `encoded.html` labeled `Encoded results`. This makes separately generated Pages reports mutually reachable without post-processing their HTML.

- [ ] **Step 5: Add the report CLI**

Add a `report` subcommand with one existing raw path plus required `--markdown` and `--html` paths. Create parent directories, write UTF-8 atomically through temporary sibling files and `Path.replace`, and return non-zero with a concise stderr message on `ReportError` or invalid pyperf JSON.

- [ ] **Step 6: Run report and CLI tests**

Run:

```bash
uv run pytest tests/test_reporting.py tests/test_cli.py -v
uv run serialization-benchmark report tests/fixtures/pyperf-smoke.json --markdown /tmp/report.md --html /tmp/report.html
```

Expected: both artifacts exist, contain two separate tables, and do not contain a composite score.

- [ ] **Step 7: Commit**

```bash
git add serialization_benchmark/reporting.py serialization_benchmark/report.css serialization_benchmark/cli.py tests
git commit -m "feat: render partitioned benchmark reports"
```

---

### Task 15: Documentation, CI, Docker, and Pages

**Files:**
- Create: `.github/workflows/benchmark-pages.yml`
- Create: `results/raw/.gitkeep`
- Create: `results/reports/.gitkeep`
- Modify: `.github/workflows/ci.yml`
- Modify: `README.md`
- Modify: `Dockerfile`
- Modify: `docker-compose.yml`
- Modify: `.gitignore`

**Interfaces:**
- Consumes: complete CLI, tests, raw report generator.
- Produces: reproducible local/container commands, matrix CI, manual canonical workflow, and Pages publication.

- [ ] **Step 1: Rewrite README against executable commands**

Replace the current library list and Docker-only instructions. Include these exact sections:

1. `What is compared`, with separate maintained primitive and encoded lists.
2. `What is not compared`, stating that format-crossing and composite scores are invalid.
3. `Methodology`, documenting canonical dataclasses, batch size 100, GC enabled, setup outside timing, correctness gating, pyperf, and payload sizes.
4. `Install and validate`:

   ```bash
   uv sync --locked
   uv run pytest
   uv run serialization-benchmark validate
   ```

5. `Run benchmarks` with primitive and encoded smoke and rigorous commands.
6. `Generate reports` using `serialization-benchmark report`.
7. `Docker` using the Compose services.
8. `Interpreting results`, warning that different machines are not directly comparable.
9. `Pickle safety`, warning never to decode untrusted pickle bytes.
10. `Adding an adapter`, requiring registry entry, exact contract validation, and format partition.

- [ ] **Step 2: Finalize Docker and Compose**

Use:

```dockerfile
FROM ghcr.io/astral-sh/uv:0.12.10 AS uv
FROM python:3.14-slim

COPY --from=uv /uv /uvx /bin/
ENV UV_COMPILE_BYTECODE=1
ENV UV_LINK_MODE=copy

WORKDIR /app
COPY pyproject.toml uv.lock README.md ./
COPY serialization_benchmark ./serialization_benchmark
COPY tests ./tests
RUN uv sync --locked

ENTRYPOINT ["uv", "run", "--locked", "serialization-benchmark"]
CMD ["validate"]
```

Keep the pinned Docker image introduced during the baseline cutover. Extend Compose with `primitive` and `encoded` services. The test service overrides the entry point with `uv run --locked pytest`. Benchmark services mount `./results:/app/results` and use smoke settings by default.

- [ ] **Step 3: Add matrix CI**

Update `ci.yml` to use `actions/checkout@v5`, `astral-sh/setup-uv@v7`, and `actions/setup-python@v6`. Matrix Python versions remain quoted `"3.12"`, `"3.13"`, and `"3.14"`. Every matrix job runs:

```bash
uv sync --locked --python "${{ matrix.python-version }}"
uv run --python "${{ matrix.python-version }}" pytest
uv run --python "${{ matrix.python-version }}" serialization-benchmark validate
```

A separate Python 3.14 smoke job runs one primitive handwritten case and one msgspec JSON case with one process, value, warmup, and loop, then runs `python -m pyperf check` on both raw files. Do not compare timing values or fail on performance variance.

- [ ] **Step 4: Add manual benchmark and Pages workflow**

Create `benchmark-pages.yml` with `workflow_dispatch` only, `pages: write` and `id-token: write` permissions, and one Python 3.14 job. It:

1. validates and tests the locked environment;
2. runs rigorous primitive and encoded suites to separate raw JSON files;
3. generates `results/site/index.md` and `results/site/index.html` from primitive data;
4. generates `results/site/encoded.md` and `results/site/encoded.html` from encoded data;
5. relies on the renderer's fixed navigation links to connect the HTML pages;
6. uploads raw JSON and reports with `actions/upload-artifact@v4`;
7. configures Pages with `actions/configure-pages@v5`;
8. uploads `results/site` with `actions/upload-pages-artifact@v4`;
9. deploys with `actions/deploy-pages@v4`.

The page header must label GitHub-hosted results as a hardware-specific snapshot, not a regression baseline.

- [ ] **Step 5: Verify obsolete automation and secrets remain absent**

Verify Travis, both shell deployment/report scripts, the encrypted deploy key, and the misspelled historical discussion file remain deleted. Ensure no README or workflow references them.

- [ ] **Step 6: Verify automation locally**

Run:

```bash
uv sync --locked
uv run pytest
uv run serialization-benchmark validate
docker compose build
docker compose run --rm validate
docker compose run --rm tests
git diff --check
```

Expected: all commands pass. Run `rg -n 'toastedmarshmallow|pystrainer|lollipop|py-kim|colander|lima==|serpy==|serpyco==' --glob '!docs/superpowers/**'` and confirm it finds no active code, dependency, or user documentation references. Confirm `.travis.yml`, `deploy-key.enc`, `deploy.sh`, and `create_report.sh` do not exist.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "ci: modernize benchmark automation and publishing"
```

---

### Task 16: Canonical Benchmark Run and Published Results

**Files:**
- Create: `results/raw/2026-09-06-cpython-3.14-primitive.json`
- Create: `results/raw/2026-09-06-cpython-3.14-encoded.json`
- Create: `results/reports/2026-09-06-cpython-3.14-primitive.md`
- Create: `results/reports/2026-09-06-cpython-3.14-primitive.html`
- Create: `results/reports/2026-09-06-cpython-3.14-encoded.md`
- Create: `results/reports/2026-09-06-cpython-3.14-encoded.html`
- Modify: `README.md`

**Interfaces:**
- Consumes: locked Python 3.14 environment and complete benchmark CLI.
- Produces: checked-in raw and rendered canonical snapshot with environment metadata.

- [ ] **Step 1: Refresh context and prove the tree is ready**

Run:

```bash
git status --short --branch
uv sync --locked --python 3.14
uv run --python 3.14 pytest
uv run --python 3.14 serialization-benchmark validate
```

Expected: only intentional uncommitted result work is present; tests and all adapters pass.

- [ ] **Step 2: Stabilize the benchmark host**

Close CPU-heavy applications, connect AC power, disable low-power mode, and record the machine model and CPU configuration. On Linux, run `uv run python -m pyperf system show` and apply available affinity/governor tuning. On macOS, retain pyperf metadata and do not claim unavailable governor control.

- [ ] **Step 3: Run the complete rigorous suites**

```bash
uv run --python 3.14 serialization-benchmark run primitive --output results/raw/2026-09-06-cpython-3.14-primitive.json -- --rigorous
uv run --python 3.14 serialization-benchmark run encoded --output results/raw/2026-09-06-cpython-3.14-encoded.json -- --rigorous
uv run --python 3.14 python -m pyperf check results/raw/2026-09-06-cpython-3.14-primitive.json
uv run --python 3.14 python -m pyperf check results/raw/2026-09-06-cpython-3.14-encoded.json
```

Expected: both suites complete, both raw files pass pyperf consistency checks, and every benchmark contains operation and environment metadata.

- [ ] **Step 4: Render reports without rerunning measurements**

```bash
uv run serialization-benchmark report results/raw/2026-09-06-cpython-3.14-primitive.json --markdown results/reports/2026-09-06-cpython-3.14-primitive.md --html results/reports/2026-09-06-cpython-3.14-primitive.html
uv run serialization-benchmark report results/raw/2026-09-06-cpython-3.14-encoded.json --markdown results/reports/2026-09-06-cpython-3.14-encoded.md --html results/reports/2026-09-06-cpython-3.14-encoded.html
```

Expected: primitive and encoded reports render from raw files alone; encoded tables show payload bytes and format partitions.

- [ ] **Step 5: Link the canonical snapshot from README**

Add links to both Markdown reports and state the exact Python version, OS, architecture, CPU model, run date, GC policy, and git revision copied from raw metadata. Do not copy timing tables into README.

- [ ] **Step 6: Run the final verification gate**

```bash
uv lock --check
uv run --python 3.12 pytest
uv run --python 3.13 pytest
uv run --python 3.14 pytest
uv run --python 3.14 serialization-benchmark validate
docker compose build
docker compose run --rm validate
git diff --check
```

Expected: every command passes. Review both HTML reports in a browser and confirm headings, units, payload sizes, prerelease labels, and links are readable.

- [ ] **Step 7: Commit the canonical snapshot**

```bash
git add README.md results/raw results/reports
git commit -m "docs: publish modern serialization benchmark results"
```

---

## Final Review Checklist

- Every active distribution is maintained and pinned at the version in Global Constraints.
- `uv.lock` resolves on Python 3.12, 3.13, and 3.14.
- Every registry entry passes exact one and batch validation.
- Every timed decode input and every schema/code generator is prepared before timing.
- Every result name and metadata record identifies tier, format when encoded, operation, batch size, adapter, version, model strategy, prerelease status, payload size when encoded, GC policy, and UTC run timestamp.
- No composite or cross-format ranking exists.
- No abandoned library, compatibility shim, Travis credential, or legacy subject remains.
- Raw results reproduce rendered Markdown and HTML without new measurements.
- Hosted timing is presented as a machine-specific snapshot.
- All commands in Task 16 pass before completion is claimed.
